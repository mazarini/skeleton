{% extends 'base.html.twig' %}

{%  block title %}
{{ entity.new ? 'New User':'Edit User' }}
{% endblock %}

{% block body %}
    <h1>{{ entity.new ? 'New User':'Edit User' }}</h1>
    {% embed '@MazariniCrud/_form.html.twig' with { 'form': form, 'entity': entity } only %}
        {% block back_href %}{{entity.new ? path('app_user_index') : path('app_user_show', {'id': entity.id}) }}{% endblock %}
        {% block return_text %}back{% endblock %}
        {% block save_text %}{{ entity.new ? 'Create':'Update' }}{% endblock %}
    {% endembed %}
{% endblock %}
