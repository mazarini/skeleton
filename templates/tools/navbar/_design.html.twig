{% set path = path('design') %}
<li class="nav-item{% if ( path == currentPath ) %} active{% endif %}">
    <a class="nav-link" href="{{ path }}">
        Design
        {%- if ( path == currentPath ) -%}
            <span class="sr-only">(current)</span>
        {%- endif -%}
    </a>
</li>
